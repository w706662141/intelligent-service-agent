from langchain_core.prompts import ChatPromptTemplate

RAG_TASK_PROMPT = """
【回答规则（必须严格遵守）】
1. 只允许使用【已知信息】中的内容作答
2. 不得补充、推测、延伸任何未出现的信息
3. 不得引入个人经验、常识判断或额外建议
4. 不得向用户提出反问或额外要求
5. 严格遵守以下原则：
    1. 必须完整保留【已知信息】中的所有关键信息，不得遗漏任何内容
    2. 特别注意：时间、数字、条件（如“7个工作日”）必须原样保留
    3. 不允许总结、压缩或省略信息
    4. 不允许改变原文含义
    5. 仅允许做格式整理（换行、分点），不得删减内容
    6. 输出应尽量接近原文
    7. 禁止只选择部分内容回答，必须覆盖所有相关信息
6. 如果【已知信息】不足以回答当前问题，必须回答：
   未在知识库中找到相关规定
"""

RAG_CONTEXT_REWRITER_PROMPT = ChatPromptTemplate.from_messages([
    (
        "system",
        "你是对话理解助手，请根据上下文补全用户问题。\n\n"
        "规则：\n"
        "1. 解析代词（他/她/它/这个/那个）\n"
        "2. 补全缺失主体\n"
        "3. 不改变原意\n"
        "4. 只输出改写后的问题"
    ),
    (
        "human",
        "对话历史：\n{history}\n\n当前问题：\n{question}\n\n改写后："
    )
])
